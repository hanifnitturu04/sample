name: Validate-YAML

on:
  push:
    branches: [ uat ]
  pull_request:
    branches: [ uat ]
    types: [opened, edited, reopened]

jobs:
  validate-yaml:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Validate YAML file
        run: yamllint *.yaml
